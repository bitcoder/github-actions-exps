name: example3c

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      uses: ./.github/actions/blabla
      with:
        entrypoint: /bin/echo $INPUT_MESSAGE
        message: Hello, world3b!
